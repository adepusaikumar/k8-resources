apiVersion: v1
kind: Pod
metadata:
  name: nginx-pod
  namespace: roboshop
spec:
  containers:
  - name: nginx
    image: nginx
    ports:
    - protocol: TCP
      containerPort: 8080
      hostPort: 8080
