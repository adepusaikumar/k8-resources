apiVersion: v1
kind: Pod
metadata:
  name: nginx-test
  labels:
    app: nginx
    environment: dev
    project: roboshop
    topic: labels
spec:
  containers:
  - name: nginx-test
    image: nginx
    ports:
    - containerPort: 80
      hostPort: 80

