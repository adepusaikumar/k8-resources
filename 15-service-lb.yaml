apiVersion: v1
kind: Service
metadata:
  name: nginx-lb
spec:
  type: LoadBalancer
  selector:
    project: roboshop
    component: frontend
    environment: dev
  ports:
    - port: 80  # service port
      targetPort: 80  # container port
      protocol: TCP
      nodePort: 32141  # host node port